#include "../transformations.h"
#include <math.h>
#include <string.h>

int main(){
    dataNur outputdata;
    char *filename = "back/tests/cube.obj";
    readFile(filename,&outputdata);
    return 0;
}