#test transformations_1
  dataNur outputdata;
  char *filename = "back/tests/cube.obj";
  readFile(filename,&outputdata);
  double originArr1[outputdata.count_of_vertexes] = {10,-10,10,-10};
  double originArr2[3] = {10,10,10};
  scalingObj(&outputdata, 10);
  rotateObj(&outputdata, 90, 'y');

  ck_assert_double_eq_tol(outputdata.count_of_vertexes, 24,1e-7);
  ck_assert_double_eq_tol(outputdata.count_of_facets, 72,1e-7);

  for(int i=0; i<outputdata.count_of_vertexes;i++)
      ck_assert_double_eq_tol(outputdata.vertexesArr[i] , originArr1[i],1e-7);
  
  for(int i=0; i<outputdata.count_of_vertexes;i++)
      ck_assert_double_eq_tol(outputdata.vertexesArr[i] , originArr2[i],1e-7);
  
  free(outputdata.vertexesArr);
  free(outputdata.facetsArr);

#test transformations_2

#test transformations_3

#test transformations_4

#test transformations_5

#test transformations_6
  